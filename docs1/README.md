# Book I, Chapter 1 — not started

Placeholder. Chapter 1 is a distinct game from Chapters 2/3 (confirmed
narratively, not just a marketing chapter split — see
[`docs23/overview.md`](../docs23/overview.md)'s "Book I backstory
recap" note) and uses a different engine, so it gets its own docs here
rather than folding into [`docs23/`](../docs23).

Once analysis begins, this should follow the same shape as `docs23/`:
an append-only `overview.md` dated session log, and a `file-formats.md`
living reference for confirmed data formats.
