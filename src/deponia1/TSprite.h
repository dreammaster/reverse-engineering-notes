// Not yet assert-confirmed to a specific file; stays at the top level.
// Referenced by TMasterControl (two TSprite members) but not itself
// exercised by any TMasterControl method reconstructed so far.
#pragma once

class TSprite {
public:
    TSprite() = default;
};
